# =============================================================================
# ENV - Backend ClubX
# =============================================================================
# La base SQLite local y el script "npm run count:local" NO necesitan este archivo.
# Solo hace falta .env si usas el servidor con MySQL o el servicio de sync.
# =============================================================================

# ----- Base de datos MySQL (servidor principal, no para modo local SQLite) -----
# DB_HOST=localhost
# DB_USERNAME=root
# DB_PASSWORD=
# DB_DATABASE=miclub_db
# DB_DIALECT=mysql

# ----- API local (opcional; por defecto usa puerto 3001) -----
# LOCAL_API_PORT=3001

# ----- Servicio de sincronización (opcional) -----
# REMOTE_API_URL=http://tu-servidor:8080/api/v1

# ----- Servidor principal (si corres npm run dev / start) -----
# HOST=0.0.0.0
# PORT=3000
# NODE_ENV=development
# CRON_ENABLE=false

# ----- Autenticación (solo si usas JWT en el servidor principal) -----
# JWT_SECRET=tu-clave-secreta
